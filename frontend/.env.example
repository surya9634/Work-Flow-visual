# For local development (when backend runs separately on port 5000)
VITE_API_URL=http://localhost:5000/api

# For production (when backend serves frontend), leave empty or use /api
# VITE_API_URL=/api
